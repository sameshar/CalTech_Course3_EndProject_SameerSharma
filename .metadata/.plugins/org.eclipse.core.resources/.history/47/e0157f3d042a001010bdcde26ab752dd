package com.BookingMyCabBuddy.service;

import java.awt.PageAttributes.MediaType;
import java.util.Optional;
import java.util.Random;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.web.bind.annotation.GetMapping;

import com.BookingMyCabBuddy.bean.Booking;
import com.BookingMyCabBuddy.bean.Customer;
import com.BookingMyCabBuddy.dao.BookingDAO;
import com.BookingMyCabBuddy.dao.CustomerDAO;

@Service
public class BookingService {

	@Autowired
	BookingDAO bookingdao;
	
	@Autowired
	CustomerService custService;
	
	@Autowired
	CustomerDAO custDAO;
	
	//method to generate a random integer for bookingID
//		public int intGenerator() {
//			Random randomintgen = new Random();
//			return randomintgen.nextInt(1000);
//		}
	
	//generate bookingID
//	int bookingID = intGenerator();
//	System.out.println(bookingID);
//	booking.setBookingID(bookingID);
	
	public String createBooking(Booking booking, Integer customerID, String email) {
			System.out.println("ServiceClass.ValidationMethod > " + booking.toString() + email + customerID);
			Optional<Integer> result = findById(customerID);
			String resultEmail = email;
			
			if (result.isEmpty()) {
				return "Customer ID entered do not exist. <a href='index.html'>Register here</a>";
			} else if (resultEmail != null) {
				boolean exists = merDAO.existsByEmail(resultEmail);
		        System.out.println("Didn't find the email address in the db? " + exists);
		        if (exists == true) {
		        	return "Continue with booking";
		        } else {
		        	return "Customer Email entered do not exist. <a href='index.html'>Register here</a>";
		        }
			}
			}
		custService.validateCustomer(booking, customerID, email)
		bookingdao.saveAndFlush(booking);	
		return "Booking Saved";
	}

}
